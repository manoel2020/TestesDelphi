unit funcoesComuns;

interface

implementation

procedure LimpaCampos(Form :String);
var
i: Integer;
begin
   for i := 0 to Self.ControlCount - 1 do
   begin
   if Self.Components[i] is TEdit then
      TEdit(Form.Components[i]).Clear;
   end;
end;
end.
